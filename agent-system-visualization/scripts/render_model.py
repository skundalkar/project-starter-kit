#!/usr/bin/env python3
"""Render a canonical agent-system model as a self-contained interactive HTML explainer."""

from __future__ import annotations

import html
import json
import sys
from pathlib import Path


TEMPLATE = r'''<!doctype html>
<html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1">
<title>__TITLE__ — agent system visualization</title>
<style>
:root{--ink:#16232b;--muted:#5b6971;--paper:#f4f1e9;--panel:#fff;--line:#9da9ae;--accent:#086b73;--accent2:#9b4c21;--soft:#e4f1ef;--warn:#fff0ce;--external:#eef0f7;--shadow:0 8px 24px rgba(22,35,43,.08)}
*{box-sizing:border-box}body{margin:0;background:var(--paper);color:var(--ink);font:15px/1.45 ui-sans-serif,system-ui,-apple-system,"Segoe UI",sans-serif}button{font:inherit}.shell{max-width:1280px;margin:auto;padding:26px 20px 44px}.kicker{font-size:.75rem;letter-spacing:.1em;text-transform:uppercase;color:var(--accent);font-weight:800}h1{font-size:clamp(1.9rem,4vw,3rem);line-height:1.05;margin:.3rem 0 .7rem}h2{font-size:1.25rem;margin:0 0 .35rem}h3{font-size:1rem;margin:0}p{margin:.3rem 0}.lead{max-width:980px;font-size:1.03rem}.takeaway{margin:18px 0;padding:15px 17px;border-left:5px solid var(--accent);background:var(--soft)}.takeaway strong{display:block}.meta,.tabs,.legend,.artifact-tags{display:flex;flex-wrap:wrap;gap:8px}.chip,.tag,.status{display:inline-flex;align-items:center;border:1px solid var(--line);border-radius:999px;padding:3px 8px;font-size:.75rem;background:var(--panel)}.tabs{margin:22px 0 14px}.tabs button{border:1px solid var(--line);background:var(--panel);color:var(--ink);padding:9px 13px;border-radius:7px;cursor:pointer}.tabs button[aria-selected=true]{background:var(--ink);color:var(--paper);border-color:var(--ink)}.view-head{display:grid;grid-template-columns:minmax(0,1fr) minmax(240px,.55fr);gap:18px;margin-bottom:13px}.view-note{color:var(--muted)}.view-takeaway{font-weight:700;border-left:3px solid var(--accent2);padding-left:12px}.legend{margin:10px 0 15px;color:var(--muted);font-size:.79rem}.legend i{width:24px;border-top:3px solid var(--accent);display:inline-block}.legend .dash{border-top-style:dashed;border-color:var(--accent2)}.workspace{display:grid;grid-template-columns:minmax(0,1.8fr) minmax(285px,.72fr);gap:18px;align-items:start}.diagram{min-width:0}.detail{position:sticky;top:12px;background:var(--panel);border:1px solid var(--line);border-radius:10px;padding:16px;box-shadow:var(--shadow)}.detail dl{margin:12px 0 0;display:grid;gap:9px}.detail dt{font-size:.7rem;text-transform:uppercase;letter-spacing:.08em;color:var(--muted)}.detail dd{margin:2px 0 0;overflow-wrap:anywhere}.detail ul{margin:3px 0 0;padding-left:18px}.groups{display:grid;gap:12px}.boundary{border:2px solid var(--ink);border-radius:10px;padding:12px;background:rgba(255,255,255,.3)}.boundary[data-boundary=external]{border-style:dashed;background:var(--external)}.boundary-head{display:flex;justify-content:space-between;gap:8px;margin-bottom:10px}.cards{display:grid;grid-template-columns:repeat(auto-fit,minmax(185px,1fr));gap:9px}.node,.artifact,.event{width:100%;text-align:left;padding:12px;border:2px solid var(--ink);border-radius:8px;background:var(--panel);color:var(--ink);cursor:pointer}.node:hover,.node:focus-visible,.artifact:hover,.artifact:focus-visible,.event:hover,.event:focus-visible{outline:3px solid #efb24f;outline-offset:2px}.node[data-kind=external_system]{border-style:dashed}.node[data-kind=decision]{border-radius:22px;border-color:var(--accent2)}.node[data-kind=artifact],.node[data-kind=store],.artifact{border-left:8px solid var(--accent)}.node[data-kind=actor],.node[data-kind=human_event]{border-left:8px solid var(--accent2)}.node[data-kind=terminal_outcome]{border-radius:999px}.kind{font-size:.7rem;text-transform:uppercase;letter-spacing:.06em;color:var(--muted);display:block;margin-bottom:5px}.desc{display:block;color:var(--muted);margin-top:5px}.relation-list{display:grid;gap:6px;margin-top:13px}.relation{display:grid;grid-template-columns:minmax(115px,1fr) minmax(160px,.9fr) minmax(115px,1fr);gap:8px;align-items:center;padding:8px 0;border-top:1px solid var(--line)}.endpoint{font-weight:700}.edge{text-align:center;color:var(--accent);font-size:.76rem;border-top:3px solid currentColor;padding:4px}.edge.context{color:var(--accent2);border-top-style:dashed}.edge small{display:block;color:var(--muted)}.scroll{overflow-x:auto;background:var(--panel);border:1px solid var(--line);border-radius:10px}.sequence-svg,.state-svg{display:block;min-width:760px;width:100%}.seq-ledger,.timeline{display:grid;gap:7px;margin-top:12px}.seq-row{display:grid;grid-template-columns:45px minmax(120px,.7fr) minmax(150px,1.6fr) minmax(130px,.8fr);gap:8px;align-items:start;padding:10px;background:var(--panel);border:1px solid var(--line);border-radius:8px}.seq-row button{all:unset;cursor:pointer;font-weight:700}.seq-row button:focus-visible{outline:3px solid #efb24f}.order{font-weight:800;color:var(--accent)}.artifact-grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(220px,1fr));gap:9px}.artifact code{display:block;margin-top:6px;white-space:normal;overflow-wrap:anywhere;color:var(--accent)}.artifact .form{font-weight:800;color:var(--accent2)}.timeline{border-left:4px solid var(--accent);padding-left:16px}.event{position:relative;border-width:1px}.event:before{content:"";position:absolute;left:-25px;top:18px;width:12px;height:12px;background:var(--accent);border:3px solid var(--paper);border-radius:50%}.event[data-type=human]:before{border-radius:2px;background:var(--accent2)}.event[data-type=terminal]{border-radius:999px}.event-top{display:flex;gap:10px;justify-content:space-between}.event-meta{color:var(--muted);font-size:.8rem}.lane-board{display:grid;gap:8px;margin:12px 0 18px;overflow-x:auto}.lane-row{display:grid;grid-template-columns:130px minmax(680px,1fr);gap:8px;align-items:stretch}.lane-name{padding:12px;background:var(--ink);color:var(--paper);border-radius:7px;font-weight:800;text-transform:capitalize}.visits{display:flex;gap:8px;align-items:stretch}.visit-card{min-width:210px;max-width:290px;text-align:left;padding:10px;border:2px solid var(--ink);border-radius:7px;background:var(--panel);color:var(--ink);cursor:pointer}.visit-card strong,.visit-card span{display:block}.visit-card:hover,.visit-card:focus-visible{outline:3px solid #efb24f;outline-offset:2px}.run-card{margin-bottom:12px;padding:13px;border:2px solid var(--ink);border-radius:9px;background:var(--panel)}.uncertainty{margin-top:24px;padding:16px;background:var(--warn);border:1px solid #d29431;border-radius:8px}.sources{margin-top:24px}.sources details{background:var(--panel);border-top:1px solid var(--line);padding:10px}.empty{padding:22px;border:1px dashed var(--line);color:var(--muted)}svg text{fill:var(--ink);font-family:ui-sans-serif,system-ui,sans-serif}svg .edge-label{stroke:var(--panel);stroke-width:5;paint-order:stroke;stroke-linejoin:round}svg .lifeline{stroke:var(--line);stroke-width:2;stroke-dasharray:5 5}svg .message{stroke:var(--accent);stroke-width:2.5}svg .message.context{stroke:var(--accent2);stroke-dasharray:7 5}svg .state-edge{stroke:var(--accent);stroke-width:2.5;fill:none}svg .state-edge.context{stroke:var(--accent2);stroke-dasharray:7 5}svg .state-node{fill:var(--panel);stroke:var(--ink);stroke-width:2}svg .terminal{rx:35}svg .decision{stroke:var(--accent2)}
.run-dashboard{overflow-x:auto;border:1px solid var(--line);border-radius:10px;background:var(--panel)}.run-dashboard svg{display:block;min-width:1480px;width:100%;height:auto}.run-dashboard .lane,.run-dashboard .artifact-lane{stroke:var(--line);stroke-width:1;opacity:.55}.run-dashboard .phase{stroke:var(--ink);stroke-width:1.25;rx:7}.run-dashboard .plan{fill:#b9d4ee}.run-dashboard .plan-critique{fill:#d6c4e7}.run-dashboard .plan-update{fill:#ead9a9}.run-dashboard .coding{fill:#b9ddcb}.run-dashboard .review{fill:#edc2bb}.run-dashboard .visit-label{font-size:13px;font-weight:750}.run-dashboard .small{font-size:11px;fill:var(--muted)}.run-dashboard .artifact-dot{fill:var(--panel);stroke:var(--ink);stroke-width:2}.run-dashboard .artifact-name{font-size:11px;font-weight:700}.run-dashboard .ribbon{fill:none;stroke:color-mix(in srgb,var(--muted) 30%,var(--line));stroke-width:.75;opacity:.45}.run-dashboard .ribbon.terminal{stroke:var(--muted);stroke-width:1.2;stroke-dasharray:4 5;opacity:.7}.run-dashboard .event-mark{stroke:var(--panel);stroke-width:2}.run-dashboard .system{fill:var(--accent2)}.run-dashboard .boundary-event{fill:#a35f3a}.run-dashboard .commit{fill:var(--accent)}.run-dashboard .hit{cursor:pointer}.run-dashboard .hit:focus{outline:none}.run-dashboard .hit:focus .phase,.run-dashboard .hit:hover .phase,.run-dashboard .hit:hover .artifact-dot{stroke:#ef9f22;stroke-width:3}.run-controls{display:flex;gap:7px;flex-wrap:wrap;margin:0 0 10px}.run-controls button{border:1px solid var(--line);border-radius:7px;background:var(--panel);padding:7px 10px;cursor:pointer}.run-controls button.active{background:var(--ink);color:var(--paper)}
@media(max-width:800px){.view-head,.workspace{grid-template-columns:1fr}.detail{position:static}.relation,.seq-row{grid-template-columns:1fr}.sequence-svg,.state-svg{min-width:680px}.shell{padding-inline:14px}}
@media(prefers-color-scheme:dark){:root{--ink:#e8eeee;--muted:#aeb9bc;--paper:#0f1719;--panel:#182326;--line:#526164;--accent:#71c8c9;--accent2:#efad79;--soft:#153638;--warn:#392c16;--external:#202536;--shadow:none}}
</style></head><body><main class="shell"><div class="kicker">Canonical agent-system model</div><h1>__TITLE__</h1><p class="lead" id="boundary"></p><div class="takeaway"><strong>What this shows</strong><span id="takeaway"></span></div><div class="meta" id="meta"></div><nav class="tabs" id="tabs" aria-label="Diagram views"></nav><section id="view"></section><section class="uncertainty"><h2>Uncertainties and boundaries</h2><ul id="uncertainties"></ul></section><section class="sources"><h2>Evidence register</h2><div id="sources"></div></section></main>
<script>
const MODEL=__DATA__;
const byId=new Map(MODEL.entities.map(x=>[x.id,x])),relById=new Map(MODEL.relationships.map(x=>[x.id,x]));
const artifactById=new Map((MODEL.artifact_records||[]).map(x=>[x.id,x])),eventById=new Map((MODEL.events||[]).map(x=>[x.id,x])),runById=new Map((MODEL.runs||[]).map(x=>[x.id,x])),sessionById=new Map((MODEL.sessions||[]).map(x=>[x.id,x]));
const $=id=>document.getElementById(id),el=(tag,value,cls)=>{const n=document.createElement(tag);if(cls)n.className=cls;if(value!==undefined)n.textContent=value;return n};
$("boundary").textContent=MODEL.system.boundary;$("takeaway").textContent=MODEL.takeaway;
[`Audience: ${MODEL.system.audience}`,`Decision: ${MODEL.system.decision}`,`Evidence cutoff: ${MODEL.system.evidence_cutoff}`].forEach(v=>$("meta").append(el("span",v,"chip")));
MODEL.uncertainties.forEach(v=>$("uncertainties").append(el("li",v)));MODEL.evidence_sources.forEach(s=>{const d=el("details"),q=el("summary",`${s.id} · ${s.path}`);d.append(q,el("p",`${s.kind} — ${s.note}`));$("sources").append(d)});
function values(v){return Array.isArray(v)?v:[v??"Unknown"]}function field(dl,label,value){const w=el("div"),dd=el("dd");w.append(el("dt",label));if(Array.isArray(value)){const ul=el("ul");value.forEach(x=>ul.append(el("li",x)));dd.append(ul)}else dd.textContent=value??"Unknown";w.append(dd);dl.append(w)}
function entityDetail(x){return {kind:x.kind,title:x.label,summary:x.responsibility,fields:[["Start reason",x.start_reason],["Stop reason",x.stop_reason],["Formal inputs",x.inputs],["Outputs",x.outputs],["Decision / control",x.decision],["Meaningful handoff",x.handoff],["Verification",x.verification],["Evidence",x.evidence],["Uncertainty",x.uncertainty]]}}
function artifactDetail(x){return {kind:`artifact · ${x.form}`,title:x.label,summary:x.location,fields:[["Persistence",x.persistence],["Verification",x.verification],["Evidence",x.evidence],["Uncertainty",x.uncertainty]]}}
function relationDetail(x){return {kind:`relationship · ${x.class}`,title:x.label,summary:x.meaning,fields:[["From",byId.get(x.from)?.label||x.from],["To",byId.get(x.to)?.label||x.to],["Formal",String(x.formal)],["Verification",x.verification],["Evidence",x.evidence]]}}
function eventDetail(x){return {kind:`event · ${x.event_type}`,title:x.label,summary:x.note,fields:[["Actor / system",x.actor],["State before",x.state_before],["State after",x.state_after],["Input artifacts",x.input_refs.map(id=>artifactById.get(id)?.label||id)],["Output artifacts",x.output_refs.map(id=>artifactById.get(id)?.label||id)],["Relationships",x.relationship_ids||[]],["Verification",x.verification],["Evidence",x.evidence]]}}
function sessionDetail(x){return {kind:`session · ${x.role}`,title:x.label,summary:x.detail,fields:[["Lane",x.lane],["Visit",String(x.visit??"not applicable")],["Start",x.start],["End",x.end],["Source",x.source],["Verification",x.verification],["Evidence",x.evidence]]}}
function detailNode(d){const box=el("aside",undefined,"detail");box.setAttribute("aria-live","polite");box.append(el("div",d.kind.replaceAll("_"," "),"kicker"),el("h2",d.title),el("p",d.summary));const dl=el("dl");d.fields.forEach(([a,b])=>field(dl,a,b));box.append(dl);return box}
function header(view,section){const h=el("div",undefined,"view-head"),l=el("div");l.append(el("div",view.type.replaceAll("_"," "),"kicker"),el("h2",view.question),el("p",view.meaning||"Derived from the canonical register.","view-note"));h.append(l,el("p",view.takeaway||MODEL.takeaway,"view-takeaway"));section.append(h);const g=el("div",undefined,"legend");g.innerHTML='<span><i></i> verified formal/control</span><span><i class="dash"></i> context or inference</span><span class="tag">labels + shapes carry meaning</span>';section.append(g)}
function nodeButton(x,on){const b=el("button",undefined,"node");b.dataset.kind=x.kind;b.append(el("span",x.kind.replaceAll("_"," "),"kind"),el("h3",x.label),el("span",x.responsibility,"desc"));b.onclick=()=>on(entityDetail(x));return b}
function relationList(ids,on){const list=el("div",undefined,"relation-list");ids.map(id=>relById.get(id)).filter(Boolean).forEach(r=>{const row=el("button",undefined,"relation");row.style.width="100%";row.style.background="transparent";row.style.borderLeft="0";row.style.borderRight="0";row.style.borderBottom="0";row.style.cursor="pointer";const edge=el("div",undefined,`edge ${r.formal&&r.verification==='observed'?'':'context'}`);edge.append(el("span",r.class.replaceAll("_"," ")),el("small",`${r.label} · ${r.id}`));row.append(el("div",byId.get(r.from)?.label||r.from,"endpoint"),edge,el("div",byId.get(r.to)?.label||r.to,"endpoint"));row.onclick=()=>on(relationDetail(r));list.append(row)});return list}
function workspace(section){const w=el("div",undefined,"workspace"),d=el("div",undefined,"diagram"),slot=el("div");w.append(d,slot);section.append(w);return {d,show:x=>slot.replaceChildren(detailNode(x))}}
function renderArchitecture(view,section){const {d,show}=workspace(section);const groups=view.groups||[{id:"system",label:"System",boundary:"inside",entities:view.entities}];groups.forEach(g=>{const box=el("section",undefined,"boundary");box.dataset.boundary=g.boundary||"inside";const h=el("div",undefined,"boundary-head");h.append(el("h3",g.label),el("span",g.boundary==="external"?"outside modeled boundary":"inside modeled boundary","status"));const cards=el("div",undefined,"cards");g.entities.map(id=>byId.get(id)).filter(Boolean).forEach(x=>cards.append(nodeButton(x,show)));box.append(h,cards);d.append(box)});d.append(relationList(view.relationships,show));const first=byId.get(view.entities[0]);if(first)show(entityDetail(first))}
function renderSequence(view,section){const {d,show}=workspace(section),parts=view.participants.map(id=>byId.get(id)).filter(Boolean),steps=[...(view.steps||[])].sort((a,b)=>a.order-b.order);const width=Math.max(760,parts.length*150),height=95+steps.length*76,ns="http://www.w3.org/2000/svg",svg=document.createElementNS(ns,"svg");svg.setAttribute("viewBox",`0 0 ${width} ${height}`);svg.setAttribute("class","sequence-svg");const defs=document.createElementNS(ns,"defs");defs.innerHTML='<marker id="arr" viewBox="0 0 10 10" refX="9" refY="5" markerWidth="6" markerHeight="6" orient="auto-start-reverse"><path d="M 0 0 L 10 5 L 0 10 z" fill="currentColor"/></marker>';svg.append(defs);const pos=new Map();parts.forEach((p,i)=>{const x=(i+.5)*width/parts.length;pos.set(p.id,x);const rect=document.createElementNS(ns,"rect");rect.setAttribute("x",String(x-62));rect.setAttribute("y","12");rect.setAttribute("width","124");rect.setAttribute("height","42");rect.setAttribute("rx","6");rect.setAttribute("class","state-node");const t=document.createElementNS(ns,"text");t.setAttribute("x",String(x));t.setAttribute("y","37");t.setAttribute("text-anchor","middle");t.setAttribute("font-size","12");t.textContent=p.label.length>20?p.label.slice(0,18)+"…":p.label;const line=document.createElementNS(ns,"line");line.setAttribute("x1",String(x));line.setAttribute("x2",String(x));line.setAttribute("y1","54");line.setAttribute("y2",String(height-12));line.setAttribute("class","lifeline");svg.append(rect,t,line)});steps.forEach((s,i)=>{const r=relById.get(s.relationship_id);if(!r)return;const x1=pos.get(r.from),x2=pos.get(r.to),y=88+i*76;if(x1===undefined||x2===undefined)return;const line=document.createElementNS(ns,"line");line.setAttribute("x1",String(x1));line.setAttribute("x2",String(x2));line.setAttribute("y1",String(y));line.setAttribute("y2",String(y));line.setAttribute("class",`message ${r.formal&&r.verification==='observed'?'':'context'}`);line.setAttribute("marker-end","url(#arr)");line.style.color="var(--accent)";const t=document.createElementNS(ns,"text");t.setAttribute("x",String((x1+x2)/2));t.setAttribute("y",String(y-9));t.setAttribute("text-anchor","middle");t.setAttribute("font-size","11");t.textContent=`${s.order}. ${r.label}`;svg.append(line,t)});const scroll=el("div",undefined,"scroll");scroll.append(svg);d.append(scroll);const ledger=el("div",undefined,"seq-ledger");steps.forEach(s=>{const r=relById.get(s.relationship_id);if(!r)return;const row=el("div",undefined,"seq-row"),btn=el("button",r.label);btn.onclick=()=>show(relationDetail(r));const refs=(s.artifact_records||[]).map(id=>artifactById.get(id)?.label||id).join("; ")||"No named artifact";row.append(el("div",String(s.order),"order"),el("div",`${byId.get(r.from)?.label||r.from} → ${byId.get(r.to)?.label||r.to}`),btn,el("div",refs,"event-meta"));ledger.append(row)});d.append(ledger);if(steps[0])show(relationDetail(relById.get(steps[0].relationship_id)))}
function renderState(view,section){const {d,show}=workspace(section),nodes=view.entities.map(id=>byId.get(id)).filter(Boolean),layout=view.layout||{},width=900,height=view.height||520,ns="http://www.w3.org/2000/svg",svg=document.createElementNS(ns,"svg");svg.setAttribute("viewBox",`0 0 ${width} ${height}`);svg.setAttribute("class","state-svg");svg.innerHTML='<defs><marker id="state-arr" viewBox="0 0 10 10" refX="9" refY="5" markerWidth="6" markerHeight="6" orient="auto"><path d="M0 0L10 5L0 10z" fill="currentColor"/></marker></defs>';view.relationships.map(id=>relById.get(id)).filter(Boolean).forEach((r,ri)=>{const a=layout[r.from],b=layout[r.to];if(!a||!b)return;const line=document.createElementNS(ns,"line");line.setAttribute("x1",String(a.x+65));line.setAttribute("y1",String(a.y+25));line.setAttribute("x2",String(b.x+65));line.setAttribute("y2",String(b.y+25));line.setAttribute("class",`state-edge ${r.formal&&r.verification==='observed'?'':'context'}`);line.setAttribute("marker-end","url(#state-arr)");line.style.color="var(--accent)";const t=document.createElementNS(ns,"text");t.setAttribute("x",String((a.x+b.x)/2+65));t.setAttribute("y",String((a.y+b.y)/2+13+((ri%3)-1)*12));t.setAttribute("text-anchor","middle");t.setAttribute("font-size","10");t.setAttribute("class","edge-label");t.textContent=r.label.length>24?r.label.slice(0,22)+"…":r.label;svg.append(line,t)});nodes.forEach(n=>{const p=layout[n.id];if(!p)return;const g=document.createElementNS(ns,"g");g.setAttribute("tabindex","0");g.style.cursor="pointer";g.onclick=()=>show(entityDetail(n));const rect=document.createElementNS(ns,"rect");rect.setAttribute("x",String(p.x));rect.setAttribute("y",String(p.y));rect.setAttribute("width","130");rect.setAttribute("height","50");rect.setAttribute("rx",n.kind==="terminal_outcome"?"25":"8");rect.setAttribute("class",`state-node ${n.kind==='decision'?'decision':''}`);const t=document.createElementNS(ns,"text");t.setAttribute("x",String(p.x+65));t.setAttribute("y",String(p.y+30));t.setAttribute("text-anchor","middle");t.setAttribute("font-size","11");t.textContent=n.label.length>19?n.label.slice(0,17)+"…":n.label;g.append(rect,t);svg.append(g)});const scroll=el("div",undefined,"scroll");scroll.append(svg);d.append(scroll,relationList(view.relationships,show));if(nodes[0])show(entityDetail(nodes[0]))}
function renderProvenance(view,section){const {d,show}=workspace(section),grid=el("div",undefined,"artifact-grid"),records=view.artifact_records.map(id=>artifactById.get(id)).filter(Boolean);records.forEach(a=>{const b=el("button",undefined,"artifact");b.append(el("span",a.form.replaceAll("_"," "),"form"),el("h3",a.label),el("code",a.location),el("span",a.persistence,"desc"));b.onclick=()=>show(artifactDetail(a));grid.append(b)});d.append(grid,relationList(view.relationships,show));if(records[0])show(artifactDetail(records[0]))}
function renderRun(view,section){const {d,show}=workspace(section),run=runById.get(view.run_id),card=el("div",undefined,"run-card");if(run){card.append(el("div",run.kind.replaceAll("_"," "),"kicker"),el("h3",run.label),el("p",run.note),el("p",`Starts: ${run.start_reason}`,"event-meta"),el("p",`Outcomes: ${run.terminal_outcomes.join('; ')}`,"event-meta"));d.append(card)}const sessions=(view.sessions||[]).map(id=>sessionById.get(id)).filter(Boolean);if(sessions.length){const board=el("div",undefined,"lane-board"),lanes=[...new Set(sessions.map(s=>s.lane))];lanes.forEach(lane=>{const row=el("div",undefined,"lane-row"),name=el("div",lane.replaceAll("-"," "),"lane-name"),visits=el("div",undefined,"visits");sessions.filter(s=>s.lane===lane).sort((a,b)=>String(a.start).localeCompare(String(b.start))).forEach(s=>{const b=el("button",undefined,"visit-card"),markers=(view.events||[]).map(id=>eventById.get(id)).filter(e=>e?.session_id===s.id&&e.event_type!=="visit");b.append(el("span",s.visit?`V${s.visit}`:s.role,"status"),el("strong",s.label),el("span",`${s.start} → ${s.end}`,"event-meta"),el("span",s.detail,"desc"));if(markers.length){const tags=el("div",undefined,"artifact-tags");markers.forEach(e=>tags.append(el("span",`${e.event_type}: ${e.label}`,"tag")));b.append(tags)}b.onclick=()=>show(sessionDetail(s));visits.append(b)});row.append(name,visits);board.append(row)});d.append(board)}const line=el("div",undefined,"timeline"),events=view.events.map(id=>eventById.get(id)).filter(Boolean).sort((a,b)=>a.order-b.order);events.forEach(e=>{const b=el("button",undefined,"event");b.dataset.type=e.event_type;const top=el("div",undefined,"event-top");top.append(el("strong",`${e.order}. ${e.label}`),el("span",e.event_type.replaceAll("_"," "),"status"));b.append(top,el("div",`${e.actor} · ${e.state_before} → ${e.state_after}`,"event-meta"),el("p",e.note));const tags=el("div",undefined,"artifact-tags");[...e.input_refs,...e.output_refs].forEach(id=>tags.append(el("span",artifactById.get(id)?.label||id,"tag")));b.append(tags);b.onclick=()=>show(eventDetail(e));line.append(b)});d.append(line);if(sessions[0])show(sessionDetail(sessions[0]));else if(events[0])show(eventDetail(events[0]))}
function renderRunDashboard(view,section){
  const {d,show}=workspace(section),run=runById.get(view.run_id),allSessions=(view.sessions||[]).map(id=>sessionById.get(id)).filter(Boolean),sessions=allSessions.filter(s=>s.visit!==null&&s.visit!==undefined),events=(view.events||[]).map(id=>eventById.get(id)).filter(Boolean);
  if(!run||!sessions.length){d.append(el("div","No timed visit evidence is available for this run.","empty"));return}
  const controls=el("div",undefined,"run-controls");["balanced","timeline","artifacts"].forEach((mode,i)=>{const b=el("button",mode==="balanced"?"Balanced":mode==="timeline"?"Timeline focus":"Artifact focus");b.className=i===0?"active":"";b.onclick=()=>{controls.querySelectorAll("button").forEach(x=>x.classList.remove("active"));b.classList.add("active");wrap.dataset.focus=mode};controls.append(b)});d.append(controls);
  const wrap=el("div",undefined,"run-dashboard");wrap.dataset.focus="balanced";const ns="http://www.w3.org/2000/svg",svg=document.createElementNS(ns,"svg"),W=1680,left=145,right=1625,laneNames=["plan","plan-critique","plan-update","coding","review","system / human","end"],laneY=new Map(laneNames.map((x,i)=>[x,105+i*76])),artifactRows=["source / requirements","plan outputs","critique outputs","update outputs","coding outputs","review outputs"],artifactY=new Map(artifactRows.map((x,i)=>[x,690+i*57])),H=1030;
  svg.setAttribute("viewBox",`0 0 ${W} ${H}`);svg.setAttribute("role","img");svg.setAttribute("aria-label",`${run.label}: time-scaled phase visits, explicit system events, formal artifacts, handoffs, and terminal state`);wrap.append(svg);d.append(wrap);
  const t0=Math.min(...sessions.map(s=>Date.parse(s.start))),t1=Math.max(...sessions.map(s=>Date.parse(s.end))),span=Math.max(1,t1-t0),x=t=>left+(right-left)*(Date.parse(t)-t0)/span,fmt=t=>new Date(t).toLocaleTimeString([], {hour:"numeric",minute:"2-digit"});
  const add=(tag,attrs,parent=svg)=>{const n=document.createElementNS(ns,tag);Object.entries(attrs||{}).forEach(([k,v])=>n.setAttribute(k,String(v)));parent.append(n);return n},txt=(value,xx,yy,cls,anchor="start",parent=svg)=>{const n=add("text",{x:xx,y:yy,class:cls||"","text-anchor":anchor},parent);n.textContent=value;return n};
  txt(`${run.label} · observed ledger`,left,34,"visit-label");txt(`${fmt(t0)} → ${fmt(t1)} · widths encode observed visit duration`,left,55,"small");
  laneNames.forEach(name=>{const y=laneY.get(name);txt(name.replaceAll("-"," "),left-14,y+4,name==="end"?"visit-label":"small","end");add("line",{x1:left,y1:y,x2:right,y2:y,class:"lane"})});artifactRows.forEach(name=>{const y=artifactY.get(name);txt(name,left-14,y+4,"small","end");add("line",{x1:left,y1:y,x2:right,y2:y,class:"artifact-lane"})});
  const sessionOut=new Map(),artifactPoints=new Map();
  const phaseOut=s=>{if(s.lane==="plan")return {id:`${s.id}:plan`,label:"plan.md",row:"plan outputs"};if(s.lane==="plan-critique")return {id:`${s.id}:critique`,label:"plan-critique.md",row:"critique outputs"};if(s.lane==="plan-update")return {id:`${s.id}:updated`,label:"updated-plan.md",row:"update outputs"};if(s.lane==="coding")return {id:`${s.id}:coding`,label:`coding-update V${s.visit}`,row:"coding outputs"};if(s.lane==="review")return {id:`${s.id}:review`,label:`review V${s.visit}`,row:"review outputs"};return null};
  const req={id:`${run.id}:requirements`,label:"requirements.md",row:"source / requirements",x:left};artifactPoints.set(req.id,{...req,y:artifactY.get(req.row)});
  sessions.sort((a,b)=>Date.parse(a.start)-Date.parse(b.start)).forEach(s=>{const y=laneY.get(s.lane);if(!y)return;const xx=x(s.start),ww=Math.max(22,x(s.end)-xx),g=add("g",{class:"hit",tabindex:0});add("rect",{x:xx,y:y-18,width:ww,height:36,class:`phase ${s.lane}`},g);txt(`${s.lane.replace("plan-","")} V${s.visit}`,xx+7,y+5,"visit-label","start",g);g.onclick=()=>show(sessionDetail(s));g.onkeydown=e=>{if(e.key==="Enter"||e.key===" ")g.onclick()};const out=phaseOut(s);if(out){const p={...out,x:x(s.end),y:artifactY.get(out.row),session:s};sessionOut.set(s.id,p);artifactPoints.set(out.id,p)}});
  const reqConsumers=sessions.filter(s=>["plan","plan-critique","plan-update","coding","review"].includes(s.lane)&&s.visit===1);const handoffs=[];reqConsumers.forEach(s=>handoffs.push({artifact:req,target:s,label:"requirements input",relationship:"formal_artifact_input"}));
  const laneVisits=lane=>sessions.filter(s=>s.lane===lane).sort((a,b)=>a.visit-b.visit),plan=laneVisits("plan")[0],crit=laneVisits("plan-critique")[0],upd=laneVisits("plan-update")[0],codings=laneVisits("coding"),reviews=laneVisits("review");if(plan&&crit)handoffs.push({artifact:sessionOut.get(plan.id),target:crit,label:"plan.md input",relationship:"formal_artifact_input"});if(plan&&upd)handoffs.push({artifact:sessionOut.get(plan.id),target:upd,label:"plan.md input",relationship:"formal_artifact_input"});if(crit&&upd)handoffs.push({artifact:sessionOut.get(crit.id),target:upd,label:"critique input",relationship:"formal_artifact_input"});if(upd&&codings[0])handoffs.push({artifact:sessionOut.get(upd.id),target:codings[0],label:"updated plan assigns Coding V1",relationship:"formal_artifact_input"});codings.forEach((c,i)=>{if(reviews[i])handoffs.push({artifact:sessionOut.get(c.id),target:reviews[i],label:`coding-update V${c.visit} input`,relationship:"formal_artifact_input"});if(i>0&&reviews[i-1])handoffs.push({artifact:sessionOut.get(reviews[i-1].id),target:c,label:`review V${reviews[i-1].visit} routes Coding V${c.visit}`,relationship:"route_decision"})});
  handoffs.filter(h=>h.artifact&&h.target).forEach((h,i)=>{const x1=h.artifact.x,y1=h.artifact.y,x2=x(h.target.start),y2=laneY.get(h.target.lane),bend=Math.min(y1-22,y2+38)-(i%5)*4,p=add("path",{d:`M ${x1} ${y1} C ${x1} ${bend}, ${x2} ${bend}, ${x2} ${y2}`,class:"ribbon hit",tabindex:0});const detail={kind:`relationship · ${h.relationship}`,title:h.label,summary:"Rendered only because the registered GraphRun contract/evidence establishes this artifact or route handoff.",fields:[["From artifact",h.artifact.label],["To visit",h.target.label],["Edge class",h.relationship],["Verification","observed"],["Evidence",h.target.evidence||[]]]};p.onclick=()=>show(detail);p.onkeydown=e=>{if(e.key==="Enter"||e.key===" ")p.onclick()}});
  [...artifactPoints.values()].forEach((a,i)=>{const g=add("g",{class:"hit",tabindex:0});add("circle",{cx:a.x,cy:a.y,r:6,class:"artifact-dot"},g);txt(a.label,a.x+(a.x>right-170?-9:9),a.y+4,"artifact-name",a.x>right-170?"end":"start",g);const record=artifactById.get(a.id.split(":").at(-1))||{form:"file",label:a.label,location:a.session?.source||"Run ledger; exact output path follows the visit directory",persistence:"Persisted per GraphRun visit.",verification:"observed",evidence:a.session?.evidence||run.evidence,uncertainty:"None"};g.onclick=()=>show(artifactDetail(record));g.onkeydown=e=>{if(e.key==="Enter"||e.key===" ")g.onclick()}});
  events.filter(e=>e.event_type!=="visit").forEach((e,i)=>{const owner=sessionById.get(e.session_id),time=e.time||(owner?new Date((Date.parse(owner.start)+Date.parse(owner.end))/2).toISOString():new Date(t0+(i+1)*span/(events.length+1)).toISOString()),xx=Math.max(left,Math.min(right,x(time))),yy=laneY.get("system / human"),kind=e.event_type==="system"?"system":e.event_type==="artifact"?"commit":"boundary-event",g=add("g",{class:"hit",tabindex:0});add("circle",{cx:xx,cy:yy,r:e.event_type==="external_boundary"?8:6,class:`event-mark ${kind}`},g);txt(e.event_type==="system"?"C":e.event_type==="artifact"?"•":"!",xx,yy+4,"small","middle",g);g.onclick=()=>show(eventDetail(e));g.onkeydown=z=>{if(z.key==="Enter"||z.key===" ")g.onclick()}});
  const lastReview=reviews.at(-1);if(lastReview){const a=sessionOut.get(lastReview.id),x2=right,y2=laneY.get("end"),p=add("path",{d:`M ${a.x} ${a.y} C ${a.x} ${a.y-35}, ${x2} ${y2+35}, ${x2} ${y2}`,class:"ribbon terminal hit",tabindex:0});add("circle",{cx:x2,cy:y2,r:8,class:"artifact-dot"});txt("terminal outcome",x2-10,y2-12,"artifact-name","end");p.onclick=()=>show({kind:"terminal transition",title:"Review routes to end",summary:run.terminal_outcomes.join("; "),fields:[["From",lastReview.label],["Route class","terminal_transition"],["Verification",run.verification],["Evidence",run.evidence]]})}
  show(sessionDetail(sessions[0]));
}
function renderGeneric(view,section){const {d,show}=workspace(section),cards=el("div",undefined,"cards"),nodes=view.entities.map(id=>byId.get(id)).filter(Boolean);nodes.forEach(n=>cards.append(nodeButton(n,show)));d.append(cards,relationList(view.relationships,show));if(nodes[0])show(entityDetail(nodes[0]))}
function render(view){document.querySelectorAll("#tabs button").forEach(b=>b.setAttribute("aria-selected",String(b.dataset.id===view.id)));const s=$("view");s.replaceChildren();header(view,s);if(view.type==="architecture")renderArchitecture(view,s);else if(view.type==="sequence")renderSequence(view,s);else if(view.type==="state"||view.type==="lifecycle")renderState(view,s);else if(view.type==="provenance")renderProvenance(view,s);else if(view.type==="run_dashboard")renderRunDashboard(view,s);else if(view.type==="run_walkthrough"||view.type==="continuity")renderRun(view,s);else renderGeneric(view,s)}
MODEL.views.forEach((v,i)=>{const b=el("button",v.label||v.type.replaceAll("_"," "));b.type="button";b.dataset.id=v.id;b.setAttribute("aria-selected",String(i===0));b.onclick=()=>render(v);$("tabs").append(b)});render(MODEL.views[0]);
</script></body></html>'''


def render(model: dict) -> str:
    title = html.escape(model["system"]["name"])
    data = json.dumps(model, ensure_ascii=False).replace("</", "<\\/")
    return TEMPLATE.replace("__TITLE__", title).replace("__DATA__", data)


def main() -> int:
    if len(sys.argv) != 3:
        print("usage: render_model.py MODEL.json OUTPUT.html", file=sys.stderr)
        return 2
    model_path, output_path = map(Path, sys.argv[1:])
    model = json.loads(model_path.read_text(encoding="utf-8"))
    output_path.parent.mkdir(parents=True, exist_ok=True)
    output_path.write_text(render(model), encoding="utf-8")
    print(f"RENDERED: {output_path}")
    return 0


if __name__ == "__main__":
    raise SystemExit(main())
